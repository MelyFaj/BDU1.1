#include<stdio.h>
int main()
{
    short int n;
    scanf("%hd", &n);
  
     if(n%2==1 || n%2==0 && n>=6 && n<=20) {
         printf("Weird");
     }
     else if(n%2==0 && n>=2 && n<=5 || n%2==0 && n>20) {
         printf("Not Weird");
     }
     return 0;
}