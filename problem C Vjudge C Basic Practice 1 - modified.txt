#include<stdio.h>
int main()
{
    short int A, B, C, X;
    scanf("%hd %hd %hd %hd", &A, &B, &C, &X);
    
     if(A==X || B==X || C==X) {
         printf("Yes");
     }
     else {
         printf("No");
     }
    return 0;
}