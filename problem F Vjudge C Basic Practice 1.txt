#include <stdio.h>
#include <math.h>
int main() {
 int b,a,h;
 float h2;
 scanf("%d%d", &b, &a);
h=(2*a)/b;
h2=(2.0*a)/b;
if(h==h2)
{
printf("%ld",h);
}
 else
{
    h=h+1;
printf("%ld",h);
}
    return 0;
}
//help taken from sifat2101033